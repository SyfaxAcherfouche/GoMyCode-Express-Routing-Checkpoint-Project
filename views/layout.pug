doctype html
html
    head
        title checkpoint | express js
        link(rel='stylesheet', href='/stylesheets/style.css')
    body
        nav.nav
            a(href="/") HOME
            a(href=`/services/${(new Date).getDay()}/${(new Date).getHours()}`) OUR SERVICES
            a(href=`/contact/${(new Date).getDay()}/${(new Date).getHours()}`) CONTACT US
            div#indicator
        block content
        block services
        block contact
